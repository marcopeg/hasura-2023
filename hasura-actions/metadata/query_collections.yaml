- name: allowed-queries
  definition:
    queries:
      - name: getChuckNorrisJoke
        query: |
          query getChuckNorrisJoke {
            chuck {
              joke: value
            }
          }
