table:
  name: users
  schema: public
select_permissions:
  - role: engineer
    permission:
      columns:
        - roles
        - name
        - id
        - created_at
        - modified_at
      filter:
        id:
          _eq: x-hasura-tenant-id
      limit: 1
      query_root_fields:
        - select
      subscription_root_fields: []
